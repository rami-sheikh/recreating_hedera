Instruction on running the experiment


git submodule init
git submodule update

cd cluster_loadgen
make

./run_experiment

./gen_plot 

Dependencies:
    - Latest Mininet (git://github.com/mininet/mininet.git, cs244 branch)
    - POX (git://github.com/noxrepo/pox)
	

Make sure to add ~/hedera to the pythonpath 

or add a symbolic link in ~/pox/ext/ using 

ln -s ~/hedera/DCController.py 
ln -s ~/hedera/HController.py
